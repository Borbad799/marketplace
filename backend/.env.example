PORT=4001
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES=7d
CLIENT_ORIGIN=http://localhost:5173
UPLOAD_DIR=uploads

# Leave empty for local SQLite. On Railway set Neon connection string.
# DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require
DB_PATH=../database/market.db

# Optional: rewrite shop demo photos on production boot
# AUTO_SEED=1
